# configs startswith '='
# no '' for string
# readme begin after ----
# log will be saved if only with more than 5 Iterations

phase=ctc

gpu=0
lr=5e-4
batch_size=2
disp_iter=40
patience=10
max_epoch=100

snap_path=DIR/snapshot
prefix=ctc_all_vgg_s

----
This is a origin ctc all_vgg_s model
Finetuned from triplet pretrained model